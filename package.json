{
  "name": "financial-app",
  "version": "1.0.0",
  "private": true,
  "description": "Financial app with frontend and backend submodules",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build ",
    "install:all": "npm --prefix frontend install "
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}